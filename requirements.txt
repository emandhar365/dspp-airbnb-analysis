# Requirements inferred from notebooks in `notebooks/`
# Core data + ML stack
pandas
numpy
scikit-learn
scipy

# Dimension reduction & manifold learning
umap-learn

# Plotting / viz
matplotlib
seaborn

# HTTP downloads used in ingestion
requests

# Parquet engines (pandas uses one of these for read_parquet/to_parquet)
pyarrow
fastparquet

# Optional: useful utilities (installed as needed by some ML packages)
joblib
threadpoolctl


#to install run command: python -m pip install -r .\requirements.txt
